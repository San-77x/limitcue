## What this changes

<!-- A short description, and the problem it solves. -->

## Why

<!-- The reasoning, if it is not obvious from the change itself. -->

## How it was verified

<!--
  e.g. `cargo clippy --all-targets -- -D warnings`, `cargo test`,
  `node scripts/test-kwin-script.js`, and any manual checks or screenshots.
-->

## Checklist

- [ ] The diff is focused on one logical change.
- [ ] `cargo clippy --all-targets -- -D warnings` passes.
- [ ] `cargo test --all-targets` passes.
- [ ] `node scripts/test-kwin-script.js` passes (if the KWin script changed).
- [ ] README or `docs/` updated where behaviour or configuration changed.
- [ ] No credentials, tokens, or personal paths are included.
